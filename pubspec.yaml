name: coastal_tourism_app
description: A mobile app for coastal tourism suitability.

publish_to: null

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0" # Latest recommended SDK range.

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5  # Latest stable version.
  http: ^1.3.0  # Updated to the latest stable version.
  cupertino_icons: ^1.0.6  # Latest stable version.
  flutter_riverpod: ^2.6.1  # Latest stable version.
  google_maps_flutter: ^2.7.0  # Latest stable version.
  flutter_spinkit: ^5.2.0  # Latest stable version.
  cloud_firestore: ^5.1.0  # Updated to the latest stable version.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1  # Latest stable version.

flutter:
  uses-material-design: true
